# Publica o Idle Office Simulator no GitHub Pages a cada push na main.
# Ative em: Settings → Pages → Source: "GitHub Actions".
# URL final: https://<usuario>.github.io/buscador-motoristas/
name: Deploy Idle Office Simulator (GitHub Pages)

on:
  push:
    branches: [main]
    paths: ['idle-office-simulator/**']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: idle-office-simulator
      - id: deployment
        uses: actions/deploy-pages@v4
